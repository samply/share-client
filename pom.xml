<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <artifactId>share-client</artifactId>
    <version>3.0.1-SNAPSHOT</version>
    <packaging>war</packaging>
    <name>Samply Share Client</name>

    <parent>
        <groupId>de.samply</groupId>
        <artifactId>parent</artifactId>
        <version>10.1</version>
    </parent>

    <profiles>
        <profile>
            <id>samply</id>
            <properties>
                <samply.project.context>samply</samply.project.context>
                <samply.project.basename>Samply.Share.Client</samply.project.basename>
            </properties>
        </profile>
        <profile>
            <id>osse</id>
            <properties>
                <samply.project.context>osse</samply.project.context>
                <samply.project.basename>OSSE.Share</samply.project.basename>
            </properties>
        </profile>
        <profile>
            <id>dktk</id>
            <properties>
                <samply.project.context>dktk</samply.project.context>
                <samply.project.basename>DKTK.Teiler</samply.project.basename>
            </properties>
        </profile>
        <profile>
            <id>gbn</id>
            <properties>
                <samply.project.context>gbn</samply.project.context>
                <samply.project.basename>GBN.Teiler</samply.project.basename>
            </properties>
        </profile>
    </profiles>

    <properties>
        <!-- general properties -->
        <database.username>samplyweb</database.username>
        <database.password>samplyweb</database.password>
        <database.url>jdbc:postgresql://localhost:5432/share_v2</database.url>
        <database.schema>samply</database.schema>

        <!-- DUMMY values to provide IntelliJ from marking errors -->
        <samply.project.context>DUMMY</samply.project.context>
        <samply.project.basename>Samply.Share.Client.DUMMY</samply.project.basename>

        <!-- Samply dependencies -->
        <share-common.version>3.0.0</share-common.version>

        <jsf-renderer.version>2.0.0</jsf-renderer.version>
        <common-mailing.version>2.0.0</common-mailing.version>
        <common-http.version>5.0.0</common-http.version>
        <common-ldmclient.centraxx.version>4.0.1</common-ldmclient.centraxx.version>
        <common-ldmclient.samplystore.version>4.0.1</common-ldmclient.samplystore.version>
        <common-ldmclient.samplystore-bbmri.version>4.0.1</common-ldmclient.samplystore-bbmri.version>

        <converter.dktk.patient.version>1.1.1</converter.dktk.patient.version>

        <!-- general dependencies -->
        <version.flyway>4.2.0</version.flyway>
        <version.jooq>3.9.5</version.jooq>
        <jta.version>1.1</jta.version>
        <quartz.version>2.3.0</quartz.version>
        <log4j-api.version>2.8</log4j-api.version>
        <omnifaces.version>1.14.1</omnifaces.version>
        <simple-xml.version>2.7.1</simple-xml.version>
        <!-- Jersey 2.x is incompatible with 1.19.4 -->
        <jersey.version>1.19.4</jersey.version>
        <jbcrypt.version>0.4</jbcrypt.version>

        <!-- webjar dependencies -->
        <jquery.version>3.3.1-1</jquery.version>
        <jquery-ui.version>1.12.1</jquery-ui.version>
        <jquery-blockui.version>2.70</jquery-blockui.version>
        <bootstrap.version>3.3.7-1</bootstrap.version>
        <bootstrap-switch.version>3.3.2</bootstrap-switch.version>
        <bootstrap-datepicker.version>1.8.0</bootstrap-datepicker.version>
        <bootstrap-fileinput.version>4.4.8</bootstrap-fileinput.version>
        <font-awesome.version>4.7.0</font-awesome.version>
        <lato.version>0.3.0</lato.version>
        <html5shiv.version>3.7.3-1</html5shiv.version>
        <respond.version>1.4.2</respond.version>
        <jasny-bootstrap.version>3.1.3-2</jasny-bootstrap.version>
        <datatables.version>1.10.16</datatables.version>
        <twbs-pagination.version>1.4.1</twbs-pagination.version>
        <jqplot.version>1.0.9.d96a669-1</jqplot.version>
    </properties>

    <build>
        <finalName>${samply.project.basename}-${project.version}</finalName>
        <resources>
            <resource>
                <directory>src/main/java</directory>
                <excludes>
                    <exclude>**/*.java</exclude>
                </excludes>
            </resource>
            <resource>
                <directory>src/main/resources</directory>
                <excludes>
                    <exclude>**/db/migration/samply/V5__test_dml.sql</exclude>
                </excludes>
            </resource>
        </resources>
        <pluginManagement>
            <plugins>
                <plugin>
                    <artifactId>maven-compiler-plugin</artifactId>
                    <configuration>
                        <compilerArgument>-Xlint:deprecation</compilerArgument>
                    </configuration>
                </plugin>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-antrun-plugin</artifactId>
                    <version>1.8</version>
                    <executions>
                        <execution>
                            <id>copy-dependencies</id>
                            <phase>compile</phase>
                            <goals>
                                <goal>run</goal>
                            </goals>
                            <configuration>
                                <target>
                                    <copy file="docs/faces-config-${samply.project.context}.xml"
                                          toFile="${project.basedir}/src/main/webapp/WEB-INF/faces-config.xml"
                                          overwrite="true"/>
                                </target>
                            </configuration>
                        </execution>
                    </executions>
                </plugin>


            </plugins>
        </pluginManagement>
        <plugins>
            <!-- JOOQ Plugin - Generate Java Classes from Database -->
            <plugin>
                <groupId>org.jooq</groupId>
                <artifactId>jooq-codegen-maven</artifactId>
                <version>${version.jooq}</version>
                <executions>
                    <execution>
                        <goals>
                            <goal>generate</goal>
                        </goals>
                    </execution>
                </executions>
                <dependencies>
                    <dependency>
                        <groupId>org.postgresql</groupId>
                        <artifactId>postgresql</artifactId>
                        <version>${version.postgres}</version>
                    </dependency>
                </dependencies>
                <configuration>
                    <jdbc>
                        <driver>org.postgresql.Driver</driver>
                        <url>${database.url}</url>
                        <user>${database.username}</user>
                        <password>${database.password}</password>
                    </jdbc>
                    <generator>
                        <database>
                            <name>org.jooq.util.postgres.PostgresDatabase</name>
                            <includes>.*</includes>
                            <excludes>samply.schema_version</excludes>
                            <inputSchema>${database.schema}</inputSchema>
                            <customTypes>
                                <customType>
                                    <name>Date</name>
                                    <type>java.util.Date</type>
                                    <converter>de.samply.share.client.util.jooq.DateConverter</converter>
                                </customType>
                            </customTypes>
                            <forcedTypes>
                                <forcedType>
                                    <name>Date</name>
                                    <types>date</types>
                                </forcedType>
                            </forcedTypes>
                        </database>
                        <generate>
                            <relations>true</relations>
                            <deprecated>true</deprecated>
                            <instanceFields>true</instanceFields>
                            <generatedAnnotation>true</generatedAnnotation>
                            <records>true</records>
                            <pojos>true</pojos>
                            <immutablePojos>false</immutablePojos>
                            <interfaces>false</interfaces>
                            <daos>true</daos>
                            <jpaAnnotations>false</jpaAnnotations>
                            <validationAnnotations>false</validationAnnotations>
                        </generate>
                        <target>
                            <packageName>de.samply.share.client.model.db</packageName>
                            <directory>src/main/java</directory>
                        </target>
                    </generator>
                </configuration>
            </plugin>
            <!-- Flyway Plugin - Create Database from SQL files -->
            <plugin>
                <groupId>org.flywaydb</groupId>
                <artifactId>flyway-maven-plugin</artifactId>
                <version>${version.flyway}</version>
                <configuration>
                    <url>${database.url}</url>
                    <user>${database.username}</user>
                    <password>${database.password}</password>
                    <schemas>
                        <schema>${database.schema}</schema>
                    </schemas>
                    <locations>
                        <location>filesystem:src/main/resources/db/migration/samply</location>
                    </locations>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-war-plugin</artifactId>
                <configuration>
                    <archive>
                        <manifestEntries>
                            <Samply-Project-Context>${samply.project.context}</Samply-Project-Context>
                        </manifestEntries>
                    </archive>
                </configuration>
            </plugin>
            <plugin>
                <artifactId>maven-source-plugin</artifactId>
                <executions>
                    <execution>
                        <id>attach-sources</id>
                        <phase>deploy</phase>
                        <goals><goal>jar-no-fork</goal></goals>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>


    <dependencies>

        <!-- JSF 2 -->
        <dependency>
            <groupId>com.sun.faces</groupId>
            <artifactId>jsf-api</artifactId>
        </dependency>
        <dependency>
            <groupId>com.sun.faces</groupId>
            <artifactId>jsf-impl</artifactId>
        </dependency>
        <dependency>
            <groupId>javax.servlet</groupId>
            <artifactId>javax.servlet-api</artifactId>
        </dependency>
        <dependency>
            <groupId>javax.transaction</groupId>
            <artifactId>jta</artifactId>
            <version>${jta.version}</version>
        </dependency>
        <!-- Samply libraries-->
        <dependency>
            <groupId>de.samply</groupId>
            <artifactId>share-common</artifactId>
            <version>${share-common.version}</version>
        </dependency>
        <dependency>
            <groupId>de.samply</groupId>
            <artifactId>common-http</artifactId>
            <version>${common-http.version}</version>
        </dependency>
        <dependency>
            <groupId>de.samply</groupId>
            <artifactId>common-ldmclient.centraxx</artifactId>
            <version>${common-ldmclient.centraxx.version}</version>
        </dependency>
        <dependency>
            <groupId>de.samply</groupId>
            <artifactId>common-ldmclient.samplystore</artifactId>
            <version>${common-ldmclient.samplystore.version}</version>
        </dependency>
        <dependency>
            <groupId>de.samply</groupId>
            <artifactId>converter.dktk.patient</artifactId>
            <version>${converter.dktk.patient.version}</version>
        </dependency>
        <dependency>
            <groupId>de.samply</groupId>
            <artifactId>jsf-renderer</artifactId>
            <version>${jsf-renderer.version}</version>
        </dependency>
        <dependency>
            <groupId>de.samply</groupId>
            <artifactId>common-mailing</artifactId>
            <version>${common-mailing.version}</version>
        </dependency>
        <dependency>
            <groupId>de.samply</groupId>
            <artifactId>common-ldmclient.samplystore-bbmri</artifactId>
            <version>${common-ldmclient.samplystore-bbmri.version}</version>
        </dependency>
        <!-- Logging -->
        <!-- TODO: Change logging to SLF4J ??? -->
        <dependency>
            <groupId>org.apache.logging.log4j</groupId>
            <artifactId>log4j-api</artifactId>
            <version>${log4j-api.version}</version>
        </dependency>
        <dependency>
            <groupId>org.apache.logging.log4j</groupId>
            <artifactId>log4j-core</artifactId>
            <version>${log4j-api.version}</version>
        </dependency>
        <dependency>
            <groupId>org.apache.logging.log4j</groupId>
            <artifactId>log4j-1.2-api</artifactId>
            <version>${log4j-api.version}</version>
        </dependency>
        <dependency>
            <groupId>org.apache.logging.log4j</groupId>
            <artifactId>log4j-slf4j-impl</artifactId>
            <version>${log4j-api.version}</version>
        </dependency>
        <dependency>
            <groupId>org.apache.logging.log4j</groupId>
            <artifactId>log4j-web</artifactId>
            <version>${log4j-api.version}</version>
            <scope>runtime</scope>
        </dependency>

        <!-- Using old JUnit 4 TODO: rewrite tests to JUnit 5 and remove this dependency-->
        <dependency>
            <groupId>junit</groupId>
            <artifactId>junit</artifactId>
        </dependency>

        <!-- Third party libraries -->
        <dependency>
            <groupId>org.omnifaces</groupId>
            <artifactId>omnifaces</artifactId>
            <version>${omnifaces.version}</version>
        </dependency>
        <dependency>
            <groupId>commons-validator</groupId>
            <artifactId>commons-validator</artifactId>
        </dependency>
        <dependency>
            <groupId>org.flywaydb</groupId>
            <artifactId>flyway-core</artifactId>
            <version>${version.flyway}</version>
        </dependency>
        <dependency>
            <groupId>org.postgresql</groupId>
            <artifactId>postgresql</artifactId>
        </dependency>
        <dependency>
            <groupId>org.jooq</groupId>
            <artifactId>jooq</artifactId>
            <version>${version.jooq}</version>
        </dependency>
        <dependency>
            <groupId>org.jooq</groupId>
            <artifactId>jooq-codegen-maven</artifactId>
            <version>${version.jooq}</version>
            <scope>compile</scope>
        </dependency>
        <dependency>
            <groupId>org.quartz-scheduler</groupId>
            <artifactId>quartz</artifactId>
            <version>${quartz.version}</version>
        </dependency>
        <dependency>
            <groupId>org.quartz-scheduler</groupId>
            <artifactId>quartz-jobs</artifactId>
            <version>${quartz.version}</version>
        </dependency>
        <dependency>
            <groupId>org.simpleframework</groupId>
            <artifactId>simple-xml</artifactId>
            <version>${simple-xml.version}</version>
        </dependency>

        <!--Jersey 1-->
        <dependency>
            <groupId>com.sun.jersey</groupId>
            <artifactId>jersey-core</artifactId>
            <version>${jersey.version}</version>
        </dependency>
        <dependency>
            <groupId>com.sun.jersey</groupId>
            <artifactId>jersey-servlet</artifactId>
            <version>${jersey.version}</version>
        </dependency>

        <dependency>
            <groupId>org.mindrot</groupId>
            <artifactId>jbcrypt</artifactId>
            <version>${jbcrypt.version}</version>
        </dependency>

        <!--Webjars from webjars.org-->
        <dependency>
            <groupId>org.webjars</groupId>
            <artifactId>jquery</artifactId>
            <version>${jquery.version}</version>
        </dependency>
        <dependency>
            <groupId>org.webjars</groupId>
            <artifactId>jquery-ui</artifactId>
            <version>${jquery-ui.version}</version>
        </dependency>
        <dependency>
            <groupId>org.webjars</groupId>
            <artifactId>bootstrap</artifactId>
            <version>${bootstrap.version}</version>
        </dependency>
        <dependency>
            <groupId>org.webjars</groupId>
            <artifactId>bootstrap-switch</artifactId>
            <version>${bootstrap-switch.version}</version>
        </dependency>
        <dependency>
            <groupId>org.webjars</groupId>
            <artifactId>bootstrap-datepicker</artifactId>
            <version>${bootstrap-datepicker.version}</version>
        </dependency>
        <dependency>
            <groupId>org.webjars</groupId>
            <artifactId>html5shiv</artifactId>
            <version>${html5shiv.version}</version>
        </dependency>
        <dependency>
            <groupId>org.webjars</groupId>
            <artifactId>respond</artifactId>
            <version>${respond.version}</version>
        </dependency>
        <dependency>
            <groupId>org.webjars</groupId>
            <artifactId>jasny-bootstrap</artifactId>
            <version>${jasny-bootstrap.version}</version>
        </dependency>
        <dependency>
            <groupId>org.webjars.bower</groupId>
            <artifactId>bootstrap-fileinput</artifactId>
            <version>${bootstrap-fileinput.version}</version>
        </dependency>
        <dependency>
            <groupId>org.webjars</groupId>
            <artifactId>font-awesome</artifactId>
            <version>${font-awesome.version}</version>
        </dependency>
        <dependency>
            <groupId>org.webjars</groupId>
            <artifactId>lato</artifactId>
            <version>${lato.version}</version>
        </dependency>
        <dependency>
            <groupId>org.webjars</groupId>
            <artifactId>datatables</artifactId>
            <version>${datatables.version}</version>
        </dependency>
        <dependency>
            <groupId>org.webjars</groupId>
            <artifactId>jquery-blockui</artifactId>
            <version>${jquery-blockui.version}</version>
        </dependency>
        <dependency>
            <groupId>org.webjars.bower</groupId>
            <artifactId>twbs-pagination</artifactId>
            <version>${twbs-pagination.version}</version>
        </dependency>
        <dependency>
            <groupId>org.webjars</groupId>
            <artifactId>jqplot</artifactId>
            <version>${jqplot.version}</version>
        </dependency>
        <dependency>
            <groupId>de.samply</groupId>
            <artifactId>common-ldmclient.samplystore-biobank</artifactId>
            <version>1.0.0</version>
        </dependency>
    </dependencies>

</project>